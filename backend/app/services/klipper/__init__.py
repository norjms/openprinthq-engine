"""Klipper / Moonraker printer support.

This package contains everything specific to controlling Klipper-based printers
(e.g. Voron) through the Moonraker API. It is intentionally self-contained so
the existing Bambu code paths are untouched.

See ``KLIPPER_SUPPORT_PLAN.md`` at the repo root for the overall design.
"""
